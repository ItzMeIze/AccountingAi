# New Website Thingy

A minimal static site scaffolded inside this workspace.

Run a simple local server and open the site:

PowerShell
```
cd new-website
python -m http.server 8000
```

Then open http://localhost:8000 in a browser (or use the provided simple browser preview).